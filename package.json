{
  "devDependencies": {
    "cypress-mailisk": "^2.0.1",
    "cypress-mailosaur": "^2.14.0",
    "typescript": "^5.4.3"
  },
  "scripts": {
    "test": "cypress run",
    "test:dev": "cypress run --headed --config-file dev.config.js",
    "test:prod": "cypress run --headed --config-file prod.config.js"
  },
  "dependencies": {
    "cypress-real-events": "^1.12.0"
  }
}
